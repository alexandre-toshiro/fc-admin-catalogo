package com.fullcycle.admin.catalogo.domain;

public abstract class ValueObject {
    /*
    A classe é abstrata e não uma interface, pois por uma interface seria possível que um objeto implementasse
    ValueObject e uma Entity. E isso não é permitido, ou é um ou o outro.
     */
}
